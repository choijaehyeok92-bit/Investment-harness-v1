# Eval — Macro Separation

Fail if:

- macro regime directly changes the 100-point company score
- a holding is exited solely because of a macro forecast
- stock-selection thesis is rewritten solely to fit macro expectations

Pass if macro is used only for portfolio risk budget, cash buffer, and purchase pacing.
